# MagicSquare RED — D-LOC-01 스켈레톤 보고서

## 문서 목적

- **Phase RED** · **Logic Track** · **entity** 레이어에서 **D-LOC-01** 의도적 실패 테스트 스켈레톤 추가 결과를 기록한다.
- 상위: [`docs/RED_Design_Table.md`](../docs/RED_Design_Table.md), [`docs/PRD.md`](../docs/PRD.md) §3 I2, §10 후속
- Transcript: [`Prompting/export-transcript-2026-06-04-red-d-loc-01-skeleton.md`](../Prompting/export-transcript-2026-06-04-red-d-loc-01-skeleton.md)

**작성 기준일:** 2026-06-04  
**Phase:** RED | **Layer:** entity | **Track:** Logic | **ID:** D-LOC-01 | **FR:** FR-LOC-01 (교안·2차, PRD 미등재)

---

## 1. 세션 요약

| 항목 | 내용 |
|------|------|
| **Command** | `/red-skeleton` |
| **Skill** | `magic-square-tdd` |
| **목표** | `find_blank_coords` RED 스켈레톤만 — `pytest.fail` 의도 실패 |
| **pytest** | `FAILED` 1 (의도된 RED) |
| **다음** | GREEN — `src/entity/`에 `find_blank_coords` 최소 구현 |

---

## 2. C2C · Given / When / Then

| Test ID | Given | When | Then (RED 스켈레톤) |
|---------|-------|------|---------------------|
| **D-LOC-01** | `G1_LOC` — `0`×2, 0-index `(1,1)`, `(2,2)` | `find_blank_coords(grid_g1)` | **1-index** `[(2,2),(3,3)]` row-major *(주석만; assert 없음)* |

**G1_LOC matrix (0-index):**

```text
[[16, 3,  2, 13],
 [ 5, 0, 11,  8],
 [ 9, 6,  0, 12],
 [ 4,15, 14,  1]]
```

**Invariant:** PRD **I2** (`0` 정확히 2개), FR-LOC-01(교안), entity **E001~E005 emit 금지**

---

## 3. pytest 결과

```bash
python -m pytest tests/entity/test_d_loc_01.py::test_d_loc_01_blank_coords_row_major -v
```

| 결과 | 값 |
|------|-----|
| collected | 1 |
| **failed** | **1** |
| passed | 0 |

**FAIL 한 줄:**

```text
Failed: RED: D-LOC-01 — 구현 없음, 의도적 실패
```

---

## 4. 변경 파일

### tests/ (본 RED 스켈레톤)

| 파일 | 역할 |
|------|------|
| `tests/conftest.py` | `grid_g1` 픽스처 — G1_LOC, `entity/constants.py` SSOT 로드 |
| `tests/entity/test_d_loc_01.py` | D-LOC-01 AAA + `pytest.fail` |

### src/ (SSOT 선행 — 픽스처 import 전제)

| 파일 | 역할 |
|------|------|
| `src/entity/constants.py` | `MAGIC_SUM=34`, `GRID_SIZE=4`, `VALUE_MAX=16` |

> `/red-skeleton` 지시에 **src/ 구현 금지**가 있었으나, **「상수는 entity/constants.py import」** 규칙 충족을 위해 **MagicConstant SSOT만** 추가함. `find_blank_coords` 등 도메인 함수는 **미구현**.

### Harness 메모

- `tests/entity/` 패키지가 `src/entity`와 이름 충돌 → `conftest`에서 `importlib`로 `constants.py` 직접 로드.

---

## 5. RED 규칙 준수 체크

| 규칙 | 상태 |
|------|------|
| AAA 주석 (Given / When / Then) | ✅ |
| Then = `pytest.fail` 한 줄만 | ✅ |
| assert / skip / xfail / 통과 더미 금지 | ✅ |
| Logic Track Domain Mock 금지 | ✅ |
| `find_blank_coords` src 구현 없음 | ✅ |

---

## 6. 다음 Phase (GREEN 힌트)

- `src/entity/` (또는 합의 모듈)에 `find_blank_coords(matrix) -> list[tuple[int,int]]` 최소 구현
- 좌표 체계: 테스트 Then 주석은 **1-index** `[(2,2),(3,3)]` — GREEN 시 0-index vs 1-index **한 체계로 assert 확정** 필요 (선행 To-Do)

---

*End of report — D-LOC-01 RED skeleton, 2026-06-04*
