# MagicSquare GREEN — D-LOC-01 보고서

## 문서 목적

- **Phase GREEN** · **Logic Track** · **entity** 레이어에서 **D-LOC-01** `find_blank_coords` 최소 구현 및 pytest PASS 결과를 기록한다.
- G1 빈칸 좌표를 PRD·교안 기준 **1-index `[(2,3),(4,4)]`** 로 정정한다 (RED 스켈레톤의 `[(2,2),(3,3)]` 은 픽스처 오류).
- 상위: [`docs/RED_Design_Table.md`](../docs/RED_Design_Table.md), [`docs/PRD.md`](../docs/PRD.md) §3 I2, §10 후속
- 선행 RED: [`03.MagicSquare_RED_D_LOC_01_Skeleton_Report.md`](03.MagicSquare_RED_D_LOC_01_Skeleton_Report.md)
- Transcript: [`Prompting/export-transcript-2026-06-04-green-d-loc-01.md`](../Prompting/export-transcript-2026-06-04-green-d-loc-01.md)

**작성 기준일:** 2026-06-04  
**Phase:** GREEN | **Layer:** entity | **Track:** Logic | **ID:** D-LOC-01 | **FR:** FR-LOC-01 (교안·2차)

---

## 1. 세션 요약

| 항목 | 내용 |
|------|------|
| **Command** | `/green-minimal` |
| **Skill** | `magic-square-tdd` |
| **목표** | `find_blank_coords` 최소 구현 · D-LOC-01 assert PASS |
| **pytest** | **1 passed** |
| **좌표 정정** | G1 Then `[(2,3),(4,4)]` — RED 주석 `[(2,2),(3,3)]` 폐기 |

---

## 2. C2C · Given / When / Then

| Test ID | Given | When | Then |
|---------|-------|------|------|
| **D-LOC-01** | `G1_LOC` — `0`×2, 0-index `(1,2)`, `(3,3)` | `find_blank_coords(grid_g1)` | **1-index** `[(2,3),(4,4)]` row-major |

**G1_LOC matrix (0-index):**

```text
[[16, 3,  2, 13],
 [ 5, 10,  0,  8],
 [ 9,  6,  7, 12],
 [ 3, 15, 14,  0]]
```

**0-index ↔ 1-index:**

| 0-index (row, col) | 1-index (row, col) |
|--------------------|--------------------|
| (1, 2) | (2, 3) |
| (3, 3) | (4, 4) |

**Invariant:** PRD **I2** (`0` 정확히 2개), entity **E001~E005 emit 금지**, ECB — entity는 boundary/control import 금지

---

## 3. REPL 스모크

```python
from entity.loc import find_blank_coords

G1 = [
    [16, 3, 2, 13],
    [5, 10, 0, 8],
    [9, 6, 7, 12],
    [3, 15, 14, 0],
]
find_blank_coords(G1)  # → [(2, 3), (4, 4)]
```

| 입력 | 기대 Then | 실제 |
|------|-----------|------|
| G1 (위 matrix) | `[(2, 3), (4, 4)]` | `[(2, 3), (4, 4)]` ✓ |

> RED 스켈레톤·초기 GREEN 시도의 `[(2,2),(3,3)]` 은 G1_LOC에 0이 0-index `(1,1)`, `(2,2)` 에 있던 **픽스처 배치 오류**에 따른 값이며, PRD G1·교안 빈칸 위치와 불일치.

---

## 4. pytest 결과

```bash
python -m pytest tests/entity/test_d_loc_01.py::test_d_loc_01_blank_coords_row_major -v
python -m pytest tests/entity/test_d_loc_01.py -v
python -m pytest tests/ -v
```

| 결과 | 값 |
|------|-----|
| collected | 1 |
| **passed** | **1** |
| failed | 0 |

---

## 5. 변경 파일

| 파일 | 역할 |
|------|------|
| `src/entity/constants.py` | `BLANK_VALUE`, `COORD_INDEX_BASE` SSOT 추가 |
| `src/entity/loc.py` | **신규** `find_blank_coords` |
| `tests/conftest.py` | G1_LOC — 0 at 0-index `(1,2)`, `(3,3)` |
| `tests/entity/test_d_loc_01.py` | GREEN assert `[(2,3),(4,4)]` |
| `tests/entity/__init__.py` | **삭제 유지** — `src/entity` import shadow 방지 |

---

## 6. 구현 요약

- **알고리즘:** matrix row-major 순회, `value == BLANK_VALUE` 인 셀을 `(row+COORD_INDEX_BASE, col+COORD_INDEX_BASE)` 로 수집.
- **매직넘버:** `0`, `1` 리터럴 없음 — `entity/constants.py` only.
- **오류 코드:** E001~E005 raise/return 없음.

---

## 7. Harness 메모

- `tests/entity/__init__.py`(빈 파일)가 있으면 pytest가 `tests.entity`를 `entity`로 shadow → `entity.loc` import 실패.
- **권장:** `tests/entity/` 에 `__init__.py` 두지 않음 (또는 후속 REFACTOR에서 테스트 패키지명 분리).

---

## 8. 다음 Phase

- **REFACTOR** (별도 지시 시): 합산·라인 순회 중복 제거 등 — 본 ID 범위 외.
- **D-LOC-02~03** (RED 설계표): 별도 RED 묶음.

---

*End of report — D-LOC-01 GREEN, 2026-06-04*
