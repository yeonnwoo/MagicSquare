# Magic Square Harness · Cursor Rules 보고서 (세션 2)

## 문서 목적

- `MagicSquare_1004` **ECB + Dual-Track TDD** 최소 Harness 구축, **`.cursorrules` 초안**, Git 배포(`spec` 브랜치) 산출을 기록한다.
- 상위 문서: [`01.MagicSquare_ProblemDefinition_Report.md`](01.MagicSquare_ProblemDefinition_Report.md), [`docs/PRD.md`](../docs/PRD.md)
- Agent 대화 원문: [`export-transcript-2026-06-04-harness-session.md`](export-transcript-2026-06-04-harness-session.md)

**작성 기준일:** 2026-06-04  
**STEP:** 2 — Harness · 프로젝트 규칙 · RED 준비  
**브랜치:** `spec` (`a2a11dd` — harness 커밋, `origin/spec` push 완료)

---

## 1. 세션 요약

| 항목 | 내용 |
|------|------|
| **목표** | pytest Harness 골격만 (구현·테스트 본문 없음) |
| **추가** | `.cursorrules` 48줄 — 도메인·ECB·Dual-Track·TDD·AI 행동 |
| **Git** | `chore: add ECB dual-track pytest harness skeleton` → `origin/spec` |
| **미커밋** | `.cursorrules`, 본 Report/02, export transcript (로컬) |

---

## 2. 산출물 — Harness

### 2.1 디렉터리 구조

```
MagicSquare_1004/
├── pyproject.toml
├── src/
│   ├── entity/__init__.py
│   ├── control/__init__.py
│   └── boundary/__init__.py
└── tests/
    ├── entity/__init__.py
    ├── control/__init__.py
    └── boundary/__init__.py
```

### 2.2 `pyproject.toml` 요약

| 항목 | 값 |
|------|-----|
| 패키지 | `magic-square-1004` 0.0.0 |
| Python | `>=3.10` (로컬 3.10.11 호환) |
| dev 의존성 | `pytest>=8.0` **만** |
| 빌드 | setuptools, `src` 레이아웃 |
| pytest | `testpaths=["tests"]`, `pythonpath=["src"]` |

### 2.3 검증

```powershell
python -m pip install -e ".[dev]"
python -m pytest tests/ -v
```

- 현재: 테스트 파일 없음 → **0 collected** (정상)
- `.gitignore`에 `*.egg-info/` 추가 (`pip install -e` 산출물 제외)

---

## 3. 산출물 — `.cursorrules` (초안)

| 섹션 | 규칙 요지 |
|------|-----------|
| **도메인** | 4×4, `0`×2, 1~16, MAGIC_SUM 34, `int[6]` 1-index, E001~E007(boundary), entity E001~E005 금지 |
| **ECB** | boundary → control → entity, entity 상위 import 금지 |
| **Dual-Track** | Logic `D-*` / `test_d_*` / Mock 금지 · UI `U-*` / `test_u_*` / Mock 허용 |
| **TDD** | RED→GREEN→REFACTOR, assert 완화·skip·xfail 금지, Phase/Layer/Track 선언 |
| **SSOT** | 34/16 등 리터럴 산재 금지 → MagicConstant 모듈 |
| **AI** | 한국어, git commit/push는 사용자 요청 시만 |

**라인 수:** 48 (요청 범위 40~80)

---

## 4. PRD·Report/01과의 정합

| PRD (1차) | Harness / Rules |
|-----------|-----------------|
| `ValidateInput`, `CheckTenLines`, `AssertComplete` | Layer: control·entity·boundary (미구현) |
| TL-01~04 RED | `tests/{entity,control,boundary}/test_d_*` · `test_u_*` (예정) |
| `E_INPUT_SIZE` 등 4종 | `.cursorrules`: boundary E001~E007, entity E001~E005 처리 금지 |
| Solver·`int[6]` 2차 | Rules에 출력 형식·1-index만 **고정** (구현은 후속) |

---

## 5. Dual-Track · ECB 매핑 (다음 RED)

| Track | Layer | 테스트 경로 | 예정 ID |
|-------|-------|-------------|---------|
| Logic | entity | `tests/entity/test_d_*.py` | D-01~ (TL 도메인) |
| Logic | control | `tests/control/test_d_*.py` | D-02~ (Command) |
| UI | boundary | `tests/boundary/test_u_*.py` | U-01~ (E001~E007) |

**Phase:** RED | **다음 작업:** `TL-01`~`04` 실패 테스트 추가 (assert 완화 금지)

---

## 6. Git 이력 (본 세션)

| 커밋 | 메시지 | 포함 |
|------|--------|------|
| `a2a11dd` | chore: add ECB dual-track pytest harness skeleton | pyproject, src/*, tests/*, .gitignore |
| — | (로컬) | `.cursorrules`, Report/02, export transcript |

**원격:** `spec` → `origin/spec`  
**PR:** https://github.com/yeonnwoo/MagicSquare/pull/new/spec

---

## 7. 후속 작업

- [ ] `.cursorrules`, Report/02, transcript 커밋 (사용자 요청 시)
- [ ] `entity/constants.py` (MagicConstant SSOT) + RED `test_d_*`
- [ ] PRD `E_*` ↔ `.cursorrules` E001~E007 매핑표 (boundary)
- [ ] Q8 재인터뷰 ([`01.magic-square-mom-test-step1-report.md`](01.magic-square-mom-test-step1-report.md))

---

## 8. 문서 추적

```
Report/01 (Mom Test·세션3) → docs/PRD.md
  → Report/02 (Harness·.cursorrules)  ← 본 문서
    → export-transcript (Agent 세션 원문 요약)
      → (다음) TL-01~04 RED
```
